@model IEnumerable<CodeFirstTeach.Models.Book>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

@foreach (var item in Model)
{
    <div class="card mb-4">
        @* 筆記( 二.4.h. ) : 在Index View中加入未上傳照片的留言之顯示方式 *@
        @if (item.Photo != null)
        {
            @* 有傳照片時，顯示的照片 *@
            <img src="~/BookPhotos/@item.Photo" class="card-img-top img-fluid" />
        }

        <div class="card-body">
            <h3>@Html.DisplayFor(modelItem => item.Title)</h3>
            @* 筆記( 二.4.j. ) : 在Index View中加入處理有換行的留言顯示方式
            ，\r\n要轉換成<br />才會顯示換行 *@
            <p>@Html.Raw(item.Description.Replace("\r\n","<br>"))</p>
        </div>
        <div class="card-footer">
            <p class="text-end">@Html.DisplayFor(modelItem => item.Author) 於 
                @Html.DisplayFor(modelItem => item.CreatedDate) 
                @* 也可以寫成@item.CreatedDate，但這個是原始資料呈現，意思是如果更改Model中的規則，這裡的資料並不會被改變規則 *@發表這則留言
                @* 因此像是照片那種沒有顯示格式問題的資料，就可以直接用@item.Photo來寫 *@
                <a asp-action="Display" asp-route-id="@item.BookID">看更多細節...</a>
            </p> 
        </div>
    </div>
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Author)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Photo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Author)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Photo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BookID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BookID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BookID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
